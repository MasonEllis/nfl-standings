server {
    listen 80;
    server_name mellis.io;

    location /nfl-standings/ {
        root /usr/local/src;
        try_files $uri $uri/ /nfl-standings/index.html;
        index index.html;
    }
}
